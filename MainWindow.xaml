<Controls:MetroWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    Title="Supersedence and Dependency Auditor"
    Width="1320" Height="820"
    MinWidth="1024" MinHeight="640"
    WindowStartupLocation="CenterScreen"
    TitleCharacterCasing="Normal"
    GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    NonActiveGlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    BorderThickness="1"
    ShowIconOnTitleBar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Steel.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Sidebar button template: shade-lift on Pressed instead of MahApps
                 Square default (which inverts colors via VSM and renders white
                 text invisible on the dark-theme sidebar). See
                 reference_srl_wpf_brand.md S11. -->
            <ControlTemplate x:Key="SidebarButtonTemplate" TargetType="Button">
                <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Margin="{TemplateBinding Padding}"
                                      RecognizesAccessKey="True"
                                      SnapsToDevicePixels="True"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="Bd" Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="#3A3A3A"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <Style x:Key="ViewButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Template" Value="{StaticResource SidebarButtonTemplate}"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="Margin" Value="8,3,8,3"/>
                <Setter Property="Padding" Value="12,0,12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background">
                    <Setter.Value><SolidColorBrush Color="#1E1E1E"/></Setter.Value>
                </Setter>
                <Setter Property="BorderBrush">
                    <Setter.Value><SolidColorBrush Color="#555555"/></Setter.Value>
                </Setter>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <Style x:Key="OptionsButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Template" Value="{StaticResource SidebarButtonTemplate}"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="Margin" Value="8,3,8,3"/>
                <Setter Property="Padding" Value="12,0,12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background">
                    <Setter.Value><SolidColorBrush Color="#1E1E1E"/></Setter.Value>
                </Setter>
                <Setter Property="BorderBrush">
                    <Setter.Value><SolidColorBrush Color="#555555"/></Setter.Value>
                </Setter>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <Style x:Key="SquareInlineButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Height" Value="28"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <Style x:Key="ViewDataGrid" TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="SelectionUnit" Value="FullRow"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="RowHeaderWidth" Value="0"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="ColumnHeaderHeight" Value="30"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}"/>
                <Setter Property="RowBackground" Value="{DynamicResource MahApps.Brushes.ThemeBackground}"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource MahApps.Brushes.Control.Background}"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource MahApps.Brushes.Gray8}"/>
            </Style>

            <Style x:Key="GridCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource MahApps.Styles.DataGridCell}">
                <Setter Property="Padding" Value="6,0,6,0"/>
            </Style>

            <Style x:Key="GridHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource MahApps.Styles.DataGridColumnHeader}">
                <Setter Property="Padding" Value="6,0,6,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>

            <Style x:Key="DetailTextStyle" TargetType="TextBox">
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="FontFamily" Value="Cascadia Code, Consolas, Courier New"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================================================================ -->
        <!-- LEFT SIDEBAR                                                     -->
        <!-- ================================================================ -->
        <Border Grid.Column="0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="14,14,14,12">
                    <TextBlock x:Name="txtAppTitle" Text="Supersedence Auditor"
                               FontSize="20" FontWeight="Bold" TextWrapping="Wrap"/>
                    <TextBlock x:Name="txtVersion" Text="v1.0.0" FontSize="10"
                               Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" Margin="14,0,14,14">
                    <Separator Margin="0,8,0,10"/>
                    <DockPanel LastChildFill="False">
                        <TextBlock x:Name="txtThemeLabel" Text="Dark Theme"
                                   DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="12"/>
                        <Controls:ToggleSwitch x:Name="toggleTheme"
                                               DockPanel.Dock="Right"
                                               IsOn="True"
                                               OnContent="" OffContent=""
                                               MinWidth="0"
                                               ToolTip="Toggle dark / light theme"/>
                    </DockPanel>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Button x:Name="btnViewSupersedence" Content="Supersedence" Style="{StaticResource ViewButtonStyle}"
                                ToolTip="Superseding / superseded application pairs with chain depth and status"/>
                        <Button x:Name="btnViewDependencies" Content="Dependencies" Style="{StaticResource ViewButtonStyle}"
                                ToolTip="Parent / dependency pairs by type (Required, Optional, App)"/>
                        <Button x:Name="btnViewBroken"       Content="Broken Rules" Style="{StaticResource ViewButtonStyle}"
                                ToolTip="Orphaned, circular, expired, disabled, missing-content, and undocumented findings"/>
                        <Button x:Name="btnViewTree"         Content="Tree View"    Style="{StaticResource ViewButtonStyle}"
                                ToolTip="Hierarchical visualization of supersedence chains and dependency trees"/>

                        <Button x:Name="btnOptions" Content="Options"
                                Style="{StaticResource OptionsButtonStyle}"
                                Margin="8,24,8,3"
                                ToolTip="Site code, SMS provider, reports folder, about"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Background="{DynamicResource MahApps.Brushes.Gray8}"/>

        <!-- ================================================================ -->
        <!-- MAIN CONTENT AREA                                                -->
        <!-- Row layout:                                                      -->
        <!--   0  module header band (view title + subtitle)                  -->
        <!--   1  action bar (Scan, filter, status filter, exports)           -->
        <!--   2  content host (4 views Visibility-swapped)                   -->
        <!--   3  GridSplitter                                                -->
        <!--   4  log drawer                                                  -->
        <!--   5  status bar                                                  -->
        <!-- ================================================================ -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="160"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource MahApps.Brushes.Gray10}" Padding="16,12,16,10">
                <StackPanel>
                    <TextBlock x:Name="txtModuleTitle" Text="Supersedence" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtModuleSubtitle"
                               Text="Configure Site / Provider in Options, then click Scan."
                               FontSize="11"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Padding="16,10,16,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnScan"
                            Grid.Column="0"
                            Content="Scan Environment"
                            Style="{StaticResource SquareInlineButtonStyle}"
                            MinWidth="160"
                            ToolTip="Bulk-load all applications and resolve every supersedence + dependency relationship in-memory"/>

                    <TextBox x:Name="txtFilter"
                             Grid.Column="1"
                             FontSize="12"
                             Padding="6,4,6,4"
                             Margin="8,0,8,0"
                             Controls:TextBoxHelper.Watermark="Filter by app name or description..."/>

                    <ComboBox x:Name="cboStatus"
                              Grid.Column="2"
                              Width="160"
                              Margin="0,0,8,0"
                              FontSize="12"
                              ToolTip="Limit rows by status">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Healthy"/>
                        <ComboBoxItem Content="Broken / Warning"/>
                        <ComboBoxItem Content="Error"/>
                    </ComboBox>

                    <Button x:Name="btnExportCsv"   Grid.Column="3" Content="Export CSV"   Style="{StaticResource SquareInlineButtonStyle}" Visibility="Collapsed"
                            ToolTip="Export the active view to CSV"/>
                    <Button x:Name="btnExportHtml"  Grid.Column="4" Content="Export HTML"  Style="{StaticResource SquareInlineButtonStyle}" Visibility="Collapsed"
                            ToolTip="Export the active view to a styled HTML report"/>
                    <Button x:Name="btnCopySummary" Grid.Column="5" Content="Copy Summary" Style="{StaticResource SquareInlineButtonStyle}" Visibility="Collapsed"
                            ToolTip="Copy a plain-text scan summary to the clipboard"/>
                </Grid>
            </Border>

            <Grid Grid.Row="2" Margin="8,0,8,0">

                <!-- ============================================================ -->
                <!-- View: Supersedence                                           -->
                <!-- ============================================================ -->
                <Grid x:Name="viewSupersedence" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="gridSupersedence"
                              Grid.Row="0"
                              Style="{StaticResource ViewDataGrid}"
                              ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                              CellStyle="{StaticResource GridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="" Width="32"
                                                Binding="{Binding StatusGlyph}"/>
                            <DataGridTextColumn Header="Superseding App" Width="2*"
                                                Binding="{Binding SupersedingApp}"/>
                            <DataGridTextColumn Header="Version" Width="90"
                                                Binding="{Binding SupersedingVersion}"/>
                            <DataGridTextColumn Header="Superseded App" Width="2*"
                                                Binding="{Binding SupersededApp}"/>
                            <DataGridTextColumn Header="Version" Width="90"
                                                Binding="{Binding SupersededVersion}"/>
                            <DataGridTextColumn Header="Depth" Width="60"
                                                Binding="{Binding ChainDepth}"/>
                            <DataGridTextColumn Header="Status" Width="*"
                                                Binding="{Binding Status}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource MahApps.Brushes.Gray8}"
                                  ResizeDirection="Rows"/>

                    <TextBox x:Name="txtSupersedenceDetail"
                             Grid.Row="2"
                             Style="{StaticResource DetailTextStyle}"
                             Text="Select a row to see relationship details."/>
                </Grid>

                <!-- ============================================================ -->
                <!-- View: Dependencies                                           -->
                <!-- ============================================================ -->
                <Grid x:Name="viewDependencies" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="gridDependencies"
                              Grid.Row="0"
                              Style="{StaticResource ViewDataGrid}"
                              ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                              CellStyle="{StaticResource GridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="" Width="32"
                                                Binding="{Binding StatusGlyph}"/>
                            <DataGridTextColumn Header="Parent App" Width="2*"
                                                Binding="{Binding ParentApp}"/>
                            <DataGridTextColumn Header="Version" Width="90"
                                                Binding="{Binding ParentVersion}"/>
                            <DataGridTextColumn Header="Dependency App" Width="2*"
                                                Binding="{Binding DependencyApp}"/>
                            <DataGridTextColumn Header="Version" Width="90"
                                                Binding="{Binding DependencyVersion}"/>
                            <DataGridTextColumn Header="Type" Width="100"
                                                Binding="{Binding DependencyType}"/>
                            <DataGridTextColumn Header="Level" Width="50"
                                                Binding="{Binding Level}"/>
                            <DataGridTextColumn Header="Status" Width="*"
                                                Binding="{Binding Status}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource MahApps.Brushes.Gray8}"
                                  ResizeDirection="Rows"/>

                    <TextBox x:Name="txtDependencyDetail"
                             Grid.Row="2"
                             Style="{StaticResource DetailTextStyle}"
                             Text="Select a row to see dependency details."/>
                </Grid>

                <!-- ============================================================ -->
                <!-- View: Broken Rules                                           -->
                <!-- ============================================================ -->
                <Grid x:Name="viewBroken" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="220"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="gridBroken"
                              Grid.Row="0"
                              Style="{StaticResource ViewDataGrid}"
                              ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                              CellStyle="{StaticResource GridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="" Width="32"
                                                Binding="{Binding SeverityGlyph}"/>
                            <DataGridTextColumn Header="Issue" Width="160"
                                                Binding="{Binding IssueType}"/>
                            <DataGridTextColumn Header="Severity" Width="90"
                                                Binding="{Binding Severity}"/>
                            <DataGridTextColumn Header="Category" Width="120"
                                                Binding="{Binding Category}"/>
                            <DataGridTextColumn Header="From App" Width="2*"
                                                Binding="{Binding FromApp}"/>
                            <DataGridTextColumn Header="To App" Width="2*"
                                                Binding="{Binding ToApp}"/>
                            <DataGridTextColumn Header="Description" Width="*"
                                                Binding="{Binding Description}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource MahApps.Brushes.Gray8}"
                                  ResizeDirection="Rows"/>

                    <TextBox x:Name="txtBrokenDetail"
                             Grid.Row="2"
                             Style="{StaticResource DetailTextStyle}"
                             Text="Select a row to see issue details and remediation."/>
                </Grid>

                <!-- ============================================================ -->
                <!-- View: Tree View                                              -->
                <!-- ============================================================ -->
                <Grid x:Name="viewTree" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <TreeView x:Name="treeRelationships"
                              Grid.Column="0"
                              FontSize="12"
                              BorderThickness="0"
                              Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
                              Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}"/>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource MahApps.Brushes.Gray8}"
                                  ResizeDirection="Columns"/>

                    <TextBox x:Name="txtTreeDetail"
                             Grid.Column="2"
                             Style="{StaticResource DetailTextStyle}"
                             Text="Select a node to see application details."/>
                </Grid>

                <!-- ============================================================ -->
                <!-- Progress overlay (scan in flight)                            -->
                <!-- ============================================================ -->
                <Border x:Name="progressOverlay"
                        Visibility="Collapsed"
                        IsHitTestVisible="False"
                        Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
                        Opacity="0.92">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Controls:ProgressRing IsActive="True" Width="56" Height="56"
                                               Foreground="{DynamicResource MahApps.Brushes.Accent}"/>
                        <TextBlock x:Name="txtProgressTitle"
                                   Text="Scanning..."
                                   FontSize="18" FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,14,0,0"/>
                        <TextBlock x:Name="txtProgressStep"
                                   Text=""
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,6,0,0"
                                   Foreground="{DynamicResource MahApps.Brushes.Gray1}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch"
                          Background="{DynamicResource MahApps.Brushes.Gray8}"
                          ResizeDirection="Rows"/>

            <Border Grid.Row="4">
                <DockPanel>
                    <TextBlock x:Name="lblLogOutput" DockPanel.Dock="Top" Text="LOG OUTPUT"
                               FontSize="10" FontWeight="SemiBold"
                               Margin="8,6,0,4"/>
                    <TextBox x:Name="txtLog"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Cascadia Code, Consolas, Courier New"
                             FontSize="11"
                             BorderThickness="0"
                             Margin="4,0,4,4"/>
                </DockPanel>
            </Border>

            <Border Grid.Row="5" Padding="8,4,8,4"
                    Background="{DynamicResource MahApps.Brushes.Gray10}">
                <TextBlock x:Name="txtStatus" Text="Ready." FontSize="11"/>
            </Border>
        </Grid>
    </Grid>

</Controls:MetroWindow>
